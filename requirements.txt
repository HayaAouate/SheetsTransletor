basic-pitch>=0.4.0
demucs>=4.1.0
music21>=9.0
streamlit>=1.30
yt-dlp>=2024.1.1
pretty_midi>=0.2.9
reportlab>=4.0
soundfile>=0.12
setuptools<81  # resampy (via basic-pitch) still imports pkg_resources, removed in setuptools 81
torchcrepe>=0.0.24  # CREPE pitch tracking (melody.py) — PyTorch, already pulled in by demucs
beat_this @ git+https://github.com/CPJKU/beat_this  # beat + downbeat tracking (beats.py); model (~77 MB) downloaded on first use
numba==0.61.2  # newer numba builds (0.67) get blocked by Windows Smart App Control ("stratégie de contrôle d'application"); this one is allowed
llvmlite==0.44.0
fastapi>=0.110  # HTTP API (api/)
uvicorn[standard]>=0.29
python-multipart>=0.0.9
